<!--
SYNC IMPACT REPORT
Version change: 1.2.0 → 1.3.0 (MINOR: 新增核心原则 + 多章节实质性扩展/收紧)
驱动来源: PR #1 代码评审（9 条）→ specs/003-identity-and-selection 定稿（2026-09-15）
Modified principles: none（I–VII 全部保留）
Added principles:
  - VIII. Determinate Reference & Stable Anchors（不猜·不空：多候选必须问、指向永不悬空、引用键必须唯一）
Modified sections:
  - 2.2 成员变更: 新增「被移除/已退出者可重新申请同一账本」
  - 2.3 默认账本与多账本: 默认账本改为「指向关系」锚点语义——不靠名字识别；改名不影响指向；
    删除默认账本须自动重建空默认账本；current_ledger_id 永不为空；回落顺序固定（默认→最近加入→报错）；
    显式选择不被兜底改写
  - 2.4 管理目标: 收紧——删去「或先确认当前账本」漏洞；多账本未指定必须列候选确认；
    账本可按编号或名称指定；同名必须列候选，禁止静默取一
  - 2.5 待审批的可得性: 扩展——申请后尽力通知 owner；通知失败/异常必须入持久化待补发记录，
    不得静默丢弃；重启后仍存在
  - Section 3 Nickname 约定: 唯一性范围由「账本内唯一」收紧为「全局唯一」（用户名模型）；
    删除「跨账本允许重名」；默认昵称防碰撞校验范围随之全局化；占用提示不得透露占用者
  - 4.1 Soft Delete: 扩展——已删除 = 只读，一切写操作必须拒绝，原删除时间不得被覆盖
Removed sections: none
Follow-up TODOs:
  - specs/001-default-nickname 与 docs/共账-agent设计方案.md 中的「账本内唯一」表述为
    历史记录，保留不改（历史 spec 是当时决策的存档）；现行真相以本 constitution +
    specs/003-identity-and-selection 为准
-->

# AI-SmartLedger Constitution

**Version**: 1.3.0 | **Ratified**: 2026-09-04 | **Last Amended**: 2026-09-15

## Core Principles (NON-NEGOTIABLE — 项目级铁律)

### I. In-Ledger Isolation
记账/查账**必须带 `ledger_id`**（用 `insert_many_for_ledger` / `query_by_ledger`），严禁用会写/查「无主账」(ledger_id=NULL) 的旧 `insert_many` / `query`。任何账本的数据只能被该账本成员看到——**账本隔离是数据安全的核心**。

### II. Identity Injection Safety
用户的 openid 身份只能通过**闭包注入**（`make_tools(openid)`）传给工具，**严禁**放进 LLM 工具 schema 或用户消息。LLM 不得看到或修改身份；管理操作按 nickname 查人，绝不依赖 LLM 传来的身份。**openid 永不对外展示**（含前缀/截断兜底）。

### III. Record Integrity
写入的账目必须**完整且合法**——金额须为有效正数、分类须在预设清单内、type 只能是 expense/income。严禁写入脏数据（空金额、非法分类、NULL ledger_id）。记账必须是**原子的**（整批全成或全不成）。

### IV. LLM Extraction Fallback
LLM 从自然语言抽取账目**不可靠**，必须兜底：抽取失败/缺金额 → 不发错误回执，而是**反问澄清**（ask_clarify）或提示重发；**绝不在兜底路径写无主账**。兜底路径写操作已收敛为一条（agent 工具），记账失败必须明确告知用户"没记上"。

### V. Agent Behavior
Agent 记账/查账后**必须确认**（回复用户结果+当前账本名）；信息不足时**必须反问**（ask_clarify）而不是猜；不确定用户在干嘛时友好提示用途。**不做超出用户意图的操作**（YAGNI，防过度设计）。

### VI. Ledger Ownership & Two-Tier Permission
账本**创建者即 owner**（唯一），其余成员为 **member**；权限**严格两级**：
- **owner 可**：审批加入申请、移除成员、改账本名、删账本
- **member 可**：记账、查账、查看本账本成员列表
**权限判定必须基于"目标账本内该成员的角色"**，不得因用户「当前/最近账本」不同而误判。管理操作一律先校验操作者在该账本内是 owner，member 调用必须被拒绝。

### VII. Approval-Based Join (审批制)
**加入账本必须经 owner 同意**：申请人凭口令提交加入申请 → 进入**待审批**状态 → owner 同意后才成为 member、才能访问该账本数据。审批前申请人**不得**看到账本任何数据。owner 拒绝则申请失效（拒绝申请为后续增强，本期预留 `rejected` 状态值；本期待审批列表只增不减，详见 spec 002）。**待审批申请必须可被 owner 查询与处理**（见 Section 2 兜底）。

### VIII. Determinate Reference & Stable Anchors (不猜·不空)
- **不猜**：凡「按名字/标识定位实体」（成员、账本、申请）命中**多个候选**时，系统 MUST 列出候选让用户选择，**禁止静默取第一个**（禁止 `LIMIT 1` 式猜测）。用户显式指定的目标 MUST 被尊重，MUST NOT 被替换为其他实体。
- **不空**：任何「指向」（当前账本、默认账本）MUST 永不为空、永不悬空。使当前账本失效的事件（被移除/退出/账本被删）MUST 触发确定性回落（优先回落到**用户自己的默认账本**）；用户显式选择的指向（含停留在已删账本的合法只读状态）MUST NOT 被兜底逻辑改写。
- **可引用**：被用于「按名字定位人」的引用键（nickname）在其被引用范围内 MUST 唯一（见 Section 3）。

## Section 2: 账本与成员机制细则

### 2.1 口令规则
- 每个账本一个**唯一口令**（随机、去易混淆字符），口令用于**识别账本并提交加入申请**。
- 口令**不是加入凭证**——拿到口令只能"申请"，最终是否加入由 owner 决定（原则 VII）。
- 口令随账本创建生成；账本软删除后其口令失效。

### 2.2 成员变更
- **移除成员**：owner 可按 nickname 移除本账本内成员；移除**立即生效**（该成员不再能看到账本数据）。被移除者的**历史账目保留在账本内**（删除成员≠删除数据），其 `created_by_user_id` 归属不变。
- **可重新申请**：被移除或已退出的用户可再次凭口令申请加入同一账本（重新走审批，与首次申请同权）。
- **改名**：owner 可改账本名。
- **删账本**：owner 可删（**软删除**，见 4.1），账目保留可追溯。
- **退出**：member 可主动退出账本（非 owner），退出后不再可见数据，历史账目同样保留。

### 2.3 默认账本与多账本
- 每个用户**自动拥有一个默认账本**（创建用户时生成，该用户为 owner），保证任何用户任何时刻至少有一个账本——机制同默认昵称。
- **默认账本由指向关系确定**（`users.default_ledger_id`），**不靠名字识别**：账本改名不影响指向；用户删除自己的默认账本时，系统 MUST 自动创建新的空默认账本并更新指向（**锚点永不落空**；自动重建仅在显式删除时发生，不在回落路径上静默执行）。
- 用户**可创建多个账本**，也可加入多个（他人）账本。
- 每个用户有**一个当前账本**（`current_ledger_id`），**永不为空**；新创建/（经同意）新加入的账本**不自动切换**，由用户显式切换。
- 当前账本因失效事件（被移除/主动退出/当前账本被删）不可用时，MUST 回落到**用户自己的默认账本**；引用丢失的兜底顺序固定为「**自己的默认账本 → 最近加入的账本 → 明确报错**」，**不得静默落入他人共享账本作为首选**。

### 2.4 管理目标（避免歧义）
- 涉及账本的管理操作（移除成员/改名/删账本/审批/重置口令/查看待审批）**必须明确作用于哪个账本**。
- 用户有**多个账本**且未指明目标时，系统 MUST 先列出账本候选（编号+名称+角色）请用户确认，**不得静默作用于当前账本**；用户**仅有一个账本**时直接执行，无需确认。
- 账本可用**编号或名称**指定；名称对应多个账本时 MUST 列出全部同名候选（含编号与成员名单）请用户选择，**禁止静默取一**（原则 VIII·不猜）。

### 2.5 待审批的可得性（兜底）与通知不丢失
- 公众号为被动响应场景：owner 未必能在申请时立即被通知。系统**必须保证 owner 主动查看时能看到全部待审批申请**（如 owner 发消息时提示"N 条待审批"），不依赖必须主动推送。
- 申请提交后系统 MUST **尽力通知** owner（含申请人昵称与账本名）；通知投递失败或生成异常时 MUST 进入**持久化的待补发记录**，**不得静默丢弃**，申请流程不得因此阻塞；待补发记录在服务重启后仍须存在，用户下次对话时补发提示。

## Section 3: Nickname 约定

昵称（nickname）是用户的**全局展示名与身份标识**（用户名模型：一个人一个名字，走遍所有账本不变），用于成员列表、按昵称定位/移除成员、审批与通知。

- **禁止显示 openid**：对外展示一律用 nickname，绝不 fallback 显示 openid（含截断前缀）。`list_ledger_members` 的 SELECT **不返回 openid**（从源头杜绝）。
- **全局唯一**：同一时刻一个昵称只属于一个用户，**跨账本亦不可重名**——昵称是「按名字定位人」的引用键（移除/审批/通知），被引用的名字必须在引用范围内唯一（原则 VIII·可引用）。设置已占用昵称 MUST 拒绝且提示不得透露占用者；改名到自身当前昵称不算冲突；数据层 MUST 维护唯一性约束（并发至多一个成功），启用约束前存量数据 MUST 无重名。
- **默认昵称**：未设置时自动生成「账本成员 + 4位随机hex」（如"账本成员 a3f9"），**用户级一个**，落库到 `users.nickname`，永不为空；防碰撞校验范围为**全局**。
- **可改不追溯归属**：用户 `set_nickname` 覆盖默认，立即生效；空/空白昵称不生效；改名不改变账目归属。（历史账目的记账人显示口径属 spec 细节，见 spec 003 FR-016。）

## Section 4: Data Lifecycle & Discipline

### 4.1 Soft Delete
账本删除采用**软删除**（`ledgers.deleted_at`），账目永不物理删除、可追溯。禁止把事务的 `ledger_id` 置 NULL（会产生不可见的孤儿数据）。
**已删除 = 只读**：对已删除账本的一切写操作（记账/改名/再次删除/重置口令/移除成员/审批加入/退出）MUST 一律拒绝并明确提示"该账本已被删除"；原删除时间 MUST NOT 被覆盖；其口令持续失效；历史账目仍可查看（带已删除提示）。

### 4.2 Idempotency (去重/幂等)
微信回调可能重复（msgid 幂等去重），**同一消息不能重复记账**。用 `_seen_cache`（msgid → True）保证同一 msgid 只处理一次。

### 4.3 YAGNI (不过度设计)
**只做用户明确要求的，不自行添加功能**。新增功能须先写规格（spec）并获用户确认，**不擅自扩大范围**。宁可少做，不做多余——过度设计是"跑偏"的根源。

## Section 5: Development Workflow

### 5.1 Tests Must Not Regress
改动不得破坏既有测试（`tests/test_wechat_mock.py`）。新增功能必须补对应测试，保持原有用例全绿。

### 5.2 Model Default Is Centralized
LLM 模型默认值统一从 `config.DEFAULT_MODEL` 读取，各文件不得硬编码。

### 5.3 Run With Clean PYTHONPATH
项目内跑 Python 时，运行命令须清空 `PYTHONPATH`（`PYTHONPATH= .venv/Scripts/python.exe ...`），避免误用外部包。

## Governance

- 本 Constitution 优先于任何临时做法；如需偏离，必须先在 `.specify/memory/constitution.md` 记录并说明理由。
- 版本由语义化版本（SemVer）管理：MAJOR = 原则删除/重定义；MINOR = 新增原则/章节；PATCH = 措辞澄清。
- 所有实现须对照本 constitution 校验合规（尤其是 `/speckit-plan` 的 Constitution Check）。本文件是运行时权威来源。
